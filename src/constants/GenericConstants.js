export default {
    // Below constants are defined because reqres.in provides token for any credentials.
    // But as per requirements user input should exact match below credentials
    EMAIL: "eve.holt@reqres.in",
    PASSWORD: "cityslicka"
}